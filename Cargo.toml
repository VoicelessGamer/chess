[package]
name = "chess"
version = "0.2.0"
authors = ["Adam Matthews <a.matthews42@outlook.com>"]
edition = "2024"

[dependencies]
serde = { version = "1.0.186", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]